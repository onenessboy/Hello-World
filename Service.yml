apiVersion: v1
kind: Service
metadata:
  name: Myweapp-service
  labels:
    app: Mywebapp-project
spec:
  selector:
    app: Mywebapp-project
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31200
